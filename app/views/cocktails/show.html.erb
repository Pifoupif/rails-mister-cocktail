<h1> <%= @cocktail.name %> </h1>
<% @cocktail.doses.each do |dose| %>
<p> <%= dose.description %>
  <span> <%= link_to 'Delete', dose_path(dose.id), method: :delete, data: { confirm: 'Are you sure?' } %></span>
</p>
<% end %>
<p>
  <%= link_to 'Create a dose', new_cocktail_dose_path(@cocktail) %>
</p>
